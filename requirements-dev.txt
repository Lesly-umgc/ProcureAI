# Dev-only dependencies for the pytest suite and CI. Production deps stay in
# requirements.txt; install both in CI: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
httpx>=0.27.0
